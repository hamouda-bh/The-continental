{% extends 'layout.twig' %}

{% block body %}
    <h1>les hotels</h1>
    {% if hotels|length == 0 %}
    Il n'y a aucun hotel à afficher.
    {% else %}
        <ul>
        {% for circuit in circuits %}
                <li>nom : {{ circuit.nom }}</li>
                <li>depart : {{ circuit.depart }}</li>
                <li>arrive  : {{ circuit.arrive }}</li>
                <li>longeur  : {{ circuit.longeur }}</li>
                <li>duree  : {{ circuit.duree }}</li>


            <br>
            <br>
        {% endfor %}
    </ul>
    {% endif %}
{% endblock %}